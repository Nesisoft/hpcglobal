# Client-side env (Vite). Copy to client/.env and fill in.
# These values are PUBLIC (shipped to the browser) — only use the anon key here.
VITE_SUPABASE_URL="https://xxxxxxxxxxxx.supabase.co"
VITE_SUPABASE_ANON_KEY="anon-public-key"
